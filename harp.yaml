dashboard:
  port: 7080

proxy:
  endpoints:
    - name: httpbin
      port: 7090
      url: http://httpbin:8080

storage:
  type: sqlalchemy
  url: postgresql+asyncpg://demo:demo@postgres/demo
  drop_tables: false
  echo: false