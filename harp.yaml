dashboard:
  port: 7080

proxy:
  endpoints:
    - name: httpbin
      port: 7090
      url: http://localhost:7091

storage:
  type: sqlalchemy
  url: postgresql+asyncpg://demo:demo@postgres/demo
  migrate: true

http_client:
  timeout: 5.0
